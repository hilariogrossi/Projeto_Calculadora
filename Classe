class Calculadora:

    def soma(self, a, b):
        return a + b

    def subtração(self, a, b):
        return a - b

    def multiplicação(self, a, b):
        return a * b

    def divisão(self, a, b):
        return a / b
